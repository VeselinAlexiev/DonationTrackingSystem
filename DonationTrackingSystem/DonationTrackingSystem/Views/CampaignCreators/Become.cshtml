@using DonationTrackingSystem.Data.Models.CampaignCreators;
@model CampaignCreatorFormModel

<h1>Become Agent</h1>
<hr />

@if (!ViewData.ModelState.IsValid && ViewData.ModelState["Error"] != null)
{
    <div class="alert alert-danger" role="alert">
        @ViewData.ModelState["Error"]?.Errors.First().ErrorMessage
    </div>
}

<div class="row">
    <div class="col-sm-12 offset-lg-2 col-lg-8 offset-xl-3 col-xl-6">
        <form method="post">
            <div class="form-group">
                <label asp-for="Username"></label>
                <input asp-for="Username" class="form-control" placeholder="Boyko Borissov">
                <span asp-validation-for="Username" class="small text-danger"></span>
            </div>
            <input class="btn btn-primary mt-3" type="submit" value="Save" />
        </form>
    </div>
</div>

@*@section Scripts{
    <partial name= "_ValidationScriptsPartial" />
}*@