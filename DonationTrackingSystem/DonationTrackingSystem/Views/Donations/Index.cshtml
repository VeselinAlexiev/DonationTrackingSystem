@model IEnumerable<DonationTrackingSystem.Web.ViewModel.Donation.Donation>

<h1>Donations</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Donator</th> <!-- Changed 'User' to 'Donator' assuming it refers to the person making the donation -->
            <th>Amount</th>
            <th>Campaign</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.DonatorId</td>
                <td>@item.Amount</td>
                <td>@item.CampaignId</td>
            </tr>
        }
    </tbody>
</table>
